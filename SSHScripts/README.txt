This folder is for scripts I can call via SSH.
Basically, function calls through SSH is a nightmare the way I wrote our SSH protocol.
Each script serves the identical method, just easier called instead of passing arguments into the function.